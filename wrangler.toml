# Cloudflare Workers configuration for Docusaurus site
name = "docs-monitor"
main = "src/worker.js"
compatibility_date = "2025-01-01"

# Assets configuration for serving static files
[assets]
directory = "./build"
binding = "ASSETS"

# Environment variables
[vars]
ENVIRONMENT = "production"

# Production environment configuration
[env.production]
name = "docs-monitor-production"
vars = { ENVIRONMENT = "production" }
# Production routes
[[env.production.routes]]
pattern = "docs3.syskit.com/monitor*"
zone_name = "syskit.com"

# Development environment configuration (local testing)
[env.dev]
name = "docs-monitor-dev"
vars = { ENVIRONMENT = "development" }

# Staging environment configuration
[env.staging]
name = "docs-monitor-staging"
vars = { ENVIRONMENT = "staging" }
# Staging routes
[[env.staging.routes]]
pattern = "docs-staging.syskit.com/monitor*"
zone_name = "syskit.com"
